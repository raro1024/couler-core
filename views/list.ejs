<!DOCTYPE html>
<html>
    <head>

    </head>
    <body>
        LIST
        <% for (var i = 0; i < data.length; i++) { %>

            <%= JSON.stringify(data[i]) %>
            <br>

            <% } %>

    </body>
</html>